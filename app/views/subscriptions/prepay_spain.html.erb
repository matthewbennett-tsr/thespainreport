<% title 'Pre-Pay Now' %>

<div class="content-left-subscription"> <%# LEFT-HAND MAIN CONTENT BLOCK %>
	<div class="" style="position: relative; z-index: 1000;">
		<h1>Prepay Now To Lock In Your Access Price</h1>
		
		<div class="article-lede" id="subscribenow">
		Original, independent reporting, editorial insight and in-depth features take you deeper into a changing country.
		</div>

		<div class="subscription-wrapper">
			<div class="left-50">
				<%= render 'prepayform_spain' %>
			</div>
			<div class="left-40-max light-green pad-20">
				<%= render 'instructions' %>
			</div>
			<div class="clear"></div>
		</div>
		
		<%= render 'ticktable' %>
	</div>
</div>
<div class="clear"></div>

<% if current_user.nil? %>
	<%= render 'creditcard_js' %>
<% elsif current_user.stripe_customer_id_spain? && current_user.credit_card_id_spain? %>

<% else %>
	<%= render 'creditcard_js' %>
<% end %>